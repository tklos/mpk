
 (1) Download routes and stops data
     - Routes
       - https://www.wroclaw.pl/open-data/dataset/rozkladjazdytransportupublicznegoplik_data
       - Copy stops.txt from OtwartyWroclaw_rozklad_jazdy_GTFS.zip to mpk/resources/
       - (Version from 23.10.2019 is in MAINTENANCE/1-add-routes-and-stops/sample-data/stops.csv)

     - Stops
       - https://www.wroclaw.pl/open-data/dataset/rozkladjazdytransportupublicznegoxml_data
       - Copy (line).xml from XML-rozkladyjazdy.zip to mpk/resources/routes/
       - Modify (line).xml
         There can be only two route versions ("wariant" tags) in the file, corresponding to the two directions of the route.
         They should have the same stop names, with reverse order.
         - Stops in one direction, e.g. "DWORZEC GŁÓWNY (Stawowa)" from line 31, might be removed or copied to the route in the
           other direction with the same "id" parameter
         - Stops with different names in the two directions can be removed or added with the combined name, e.g.
           "Poczta Główna / Urząd Wojewódzki" from line D
         - "numer" and "czas" arguments are not read
       - (0031.xml from 23.10.2019 is in MAINTENANCE/1-add-routes-and-stops/sample-data/0031.xml)


 (2) Process
     - Add routes and stops to the db
       (env) $ ./manage.py add_route (line)

     - If needed, modify stops_stop's display_name
       You can use \u2191 and \u2193 or ↑ and ↓ for upwards and downwards arrow to mark stops in only one direction.
       (Stop in one direction in the first direction of the route should be marked with the upwards arrow)
       Newlines and unicode characters may be added with Postgres E strings.

